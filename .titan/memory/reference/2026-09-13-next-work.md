# Next implementation block

Date: 2026-09-13
Last updated: 2026-09-21
Status: pending external acceptance
Source: Phase 7 pilot acceptance of `.titan/plans/prp-vision-studio-mvp.md`.

## Completed handoff

Phase 7 platform controls are deployed: atomic tenant contracts, module changes,
usage counters and time-bounded audited assistance. Its remote fixture passed.
The platform dashboard/organization-directory split, role-aware navigation, mobile
drawer, catalog editing refinements and targeted UTF-8 data repair are also complete.
The protected `/manual` documentation generator, operational/administrative
content variants, Markdown/PDF export paths and public landing/login captures are
implemented. Anonymous desktop/mobile access checks passed.
The remaining MVP work is interactive authenticated pilot acceptance.

## Next objective

Run and sign off the complete Cuba pilot journey with real test accounts and
responsive browser coverage.

## Implementation order

1. Validate `/dashboard`, `/organizations`, `/production`, `/orders` and `/catalog`
   with platform administrator, owner, seller and both provider roles, confirming
   that every role sees only relevant navigation links.
2. Exercise onboarding, customer/prescription, sale, payment, production, incident,
   rework, notification preparation, delivery and cashbox closure end to end.
3. Repeat critical views at mobile, tablet and desktop widths. Include the hamburger
   drawer, dialog containment, select chevrons, ArrowUp/ArrowDown behavior, Escape,
   Tab order and focus restoration.
4. Rehearse migration recovery against a disposable environment or approved backup.
5. Open `/manual` as an owner and platform administrator; verify editing,
   reordering, reset and both exports, then render both PDF variants to inspect
   pagination and clipping. Capture authenticated operational screens only with
   authorized test accounts.
6. Define OpenWA transport and retry behavior only after connection details arrive.

## External follow-ups

- Enable leaked-password protection in the Supabase Auth dashboard.
- OpenWA connection details and retry policy remain deferred until supplied.
