# LensSpace public SEO verification

Date: 2026-09-21

## Local checks

- `npm run typecheck`: passed.
- `npm run lint`: passed.
- `npm run build`: passed; `/`, `/robots.txt`, `/sitemap.xml`,
  `/opengraph-image` and `/twitter-image` are statically generated.
- `/`: HTTP 200 with description, canonical, Open Graph, Twitter card and
  JSON-LD markers.
- `/robots.txt`: HTTP 200 with canonical sitemap and private-route exclusions.
- `/sitemap.xml`: HTTP 200 and contains only `https://lensspace.vercel.app`.
- `/login`: HTTP 200 and contains `noindex`.
- `/opengraph-image`: HTTP 200 `image/png`; visual evidence is
  `seo-opengraph-card.png` at 1200 × 630.

## Production observation

On 2026-09-21, `https://lensspace.vercel.app` resolved successfully but still
redirected `/` to `/dashboard` and then `/login`, demonstrating that the new
landing/SEO build had not yet been deployed at the time of this verification.

## Sources applied

- Next.js 16 metadata, Open Graph image, robots and sitemap conventions.
- Google Search Central guidance for descriptive metadata, canonicals,
  crawlable links, robots and root sitemaps.
- Schema.org WebApplication vocabulary.
